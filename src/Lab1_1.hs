-- 1. Наведіть приклади нетривіальних виразів такого типу:
-- 1) ((Char,Integer), String, [Double])
-- Нетривіальність означає, що наявні у виразах списки містять більше одного
-- элемента.

module Lab1_1 where

-- lab1_1 :: ((Char, Integer), String, [Double])
lab1_1 = (('a',1),"aaabb",[1.1])